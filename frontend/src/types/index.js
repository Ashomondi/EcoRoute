/**
 * Shared API contracts — JSDoc typedefs mirroring `backend/internal/models`.
 *
 * @typedef {import('./user').User} User
 * @typedef {import('./user').RegisterInput} RegisterInput
 * @typedef {import('./wastePoint').WastePoint} WastePoint
 * @typedef {import('./wastePoint').WastePointInput} WastePointInput
 * @typedef {import('./wastePoint').AIPrediction} AIPrediction
 * @typedef {import('./truck').Truck} Truck
 * @typedef {import('./truck').TruckInput} TruckInput
 * @typedef {import('./route').Route} Route
 * @typedef {import('./route').RouteStop} RouteStop
 * @typedef {import('./route').OptimizationResult} OptimizationResult
 * @typedef {import('./collection').CollectionRecord} CollectionRecord
 * @typedef {import('./report').WasteReport} WasteReport
 * @typedef {import('./report').ReportInput} ReportInput
 * @typedef {import('./community').CommunitySummary} CommunitySummary
 * @typedef {import('./community').ActivityItem} ActivityItem
 * @typedef {import('./analytics').AnalyticsSummary} AnalyticsSummary
 * @typedef {import('./analytics').DailySummary} DailySummary
 */

export {}
